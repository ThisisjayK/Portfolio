# Dependencies
node_modules/

# Local env
*.local
.env
.env.*

# Editor / OS cruft
.DS_Store
*.log

# Vite cache
.vite/

# Built site — GitHub Actions rebuilds and deploys this on every push, so it
# is not committed. Run `npm run build` locally to regenerate it.
docs/

# Generated by scripts/sync-assets.mjs from resume/resume.pdf, which is the one
# tracked copy. Committing both let them drift: the public/ copy was made by
# hand, so editing resume.tex and forgetting to re-copy shipped a stale resume.
public/jayanth-resume.pdf
