# Dependencies
node_modules/

# Local env
*.local
.env
.env.*

# Editor / OS cruft
.DS_Store
*.log

# Vite cache
.vite/

# Built site — GitHub Actions rebuilds and deploys this on every push, so it
# is not committed. Run `npm run build` locally to regenerate it.
docs/
